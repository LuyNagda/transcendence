{
  "scripts": {
    "build": "NODE_ENV=production node esbuild.config.js",
    "dev": "NODE_ENV=development node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "gl-matrix": "^3.4.3",
    "htmx.org": "^1.9.12",
    "regl": "^2.1.1"
  }
}