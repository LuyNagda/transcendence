name: unit tests

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup env
        run: cp transcendence/.exampleenv transcendence/.env

      - name: Run Tests
        run: make test