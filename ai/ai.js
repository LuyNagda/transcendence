function dot(weights, inputs, biases, output) {
    let x = weights.length
    let y = weights[0].length

    for (var i = 0; i < x; ++i) {
        // output[i] = 
    }

}

class Layer_Dense {
    constructor(weights) {
        this.weights = weights
    }

    forward(inputs){
        // this.output = 
    }
}